<Page x:Class="InventoryPC.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:InventoryPC.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="1200">
    <Page.DataContext>
        <vm:MainViewModel />
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Content="Обновить" Command="{Binding RefreshCommand}" Margin="10" Width="100" />
        <ProgressBar Grid.Row="1" Value="{Binding ProgressValue}" Visibility="{Binding IsProgressVisible, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     Minimum="0" Maximum="100" Height="20" Margin="10" />
        <DataGrid Grid.Row="2" ItemsSource="{Binding Computers}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя ПК" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Windows" Binding="{Binding WindowsVersion}" Width="*" />
                <DataGridTextColumn Header="Активация" Binding="{Binding ActivationStatus}" Width="*" />
                <DataGridTextColumn Header="Срок лицензии" Binding="{Binding LicenseExpiry}" Width="*" />
                <DataGridTextColumn Header="IP-адрес" Binding="{Binding IPAddress}" Width="*" />
                <DataGridTextColumn Header="MAC-адрес" Binding="{Binding MACAddress}" Width="*" />
                <DataGridTextColumn Header="Маска подсети" Binding="{Binding SubnetMask}" Width="*" />
                <DataGridTextColumn Header="Шлюз" Binding="{Binding Gateway}" Width="*" />
                <DataGridTextColumn Header="DNS-серверы" Binding="{Binding DNSServers}" Width="*" />
                <DataGridTextColumn Header="ОЗУ" Binding="{Binding Memory}" Width="*" />
                <DataGridTextColumn Header="Процессор" Binding="{Binding Processor}" Width="*" />
                <DataGridTextColumn Header="Мониторы" Binding="{Binding Monitors}" Width="*" />
                <DataGridTextColumn Header="Мышь" Binding="{Binding Mouse}" Width="*" />
                <DataGridTextColumn Header="Клавиатура" Binding="{Binding Keyboard}" Width="*" />
                <DataGridTextColumn Header="Office" Binding="{Binding OfficeStatus}" Width="*" />
                <DataGridTextColumn Header="Office Лицензия" Binding="{Binding OfficeLicenseName}" Width="*" />
                <DataGridTextColumn Header="Дата проверки" Binding="{Binding LastChecked}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>